# ossim-scripts

## how to install
run the follow commands:  
      `apt install ansible`  
      `ssh-agent bash`  
      `ssh-add ~/.ssh/id_rsa`  
  

## how to use
  `ansible-playbook initScript.yml`
